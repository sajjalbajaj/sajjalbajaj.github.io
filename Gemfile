# For OPTIONAL local preview only. GitHub Pages ignores this file and builds
# with its own `github-pages` gem, so you don't need Ruby installed to publish.
#
# To preview locally (if you install Ruby):
#   gem install bundler
#   bundle install
#   bundle exec jekyll serve   # then open http://localhost:4000

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
